<?php
/*
 * views/pages/checkout.php
 *
 * TODO: Implement the checkout page.
 *
 * Expected variables from OrderController::showCheckout():
 *   $cartItems (array) — cart items with product info
 *   $cartTotal (float) — order total
 *   $user      (array) — current logged-in user
 *   $errors    (array) — validation errors (on POST failure)
 *
 * Future logic:
 *   - Display an order summary (items + total)
 *   - Show a shipping details form (name, address, email)
 *   - Submit via POST /checkout/process
 *   - Include a CSRF token on the form
 *   - Redirect to an order confirmation page on success
 *
 * ACCESSIBILITY: All form inputs must have a <label>. Required fields need
 *               both `required` and `aria-required="true"`.
 */

// TODO: require layout and render checkout content here
