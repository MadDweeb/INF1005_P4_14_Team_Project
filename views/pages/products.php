<?php
/*
 * views/pages/products.php
 *
 * TODO: Implement the product catalogue page.
 *
 * Expected variables from ProductController::index():
 *   $products    (array) — paginated list of products
 *   $totalPages  (int)   — total number of pages
 *   $currentPage (int)   — current page number
 *
 * Future logic:
 *   - Display product cards in a responsive grid
 *   - Filter by switch type (linear, tactile, clicky) via GET parameters
 *   - Filter by price range via GET parameters
 *   - Search by name/description via GET parameter 'q'
 *   - Pagination component
 *
 * ACCESSIBILITY: Each product card needs an accessible name and a focusable link.
 *               Filter controls need <label> elements.
 */

// TODO: require layout and render product catalogue content here
