<?php
/*
 * views/pages/cart.php
 *
 * TODO: Implement the shopping cart page.
 *
 * Expected variables from CartController::index():
 *   $cartItems (array) — cart item rows joined with product info
 *   $cartTotal (float) — pre-calculated order total
 *
 * Future logic:
 *   - Display each cart item with product name, quantity, unit price, subtotal
 *   - Allow the user to update quantities (POST /cart/update)
 *   - Allow the user to remove items (POST /cart/remove)
 *   - Show the order total and a "Proceed to Checkout" button
 *   - Include CSRF tokens on all POST forms
 *
 * ACCESSIBILITY: Use a <table> with <th scope="col"> headers for cart items.
 *               Each quantity input needs a <label> that includes the product name.
 */

// TODO: require layout and render cart content here
